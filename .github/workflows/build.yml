name: Build Lupus Workflow Images

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build_docker:
    name: Build and push Docker image to GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Check out the repo
        uses: actions/checkout@v5

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            ghcr.io/${{ github.repository }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  build_singularity:
    name: Build and push Singularity image to GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Check out the repo
        uses: actions/checkout@v5

      - name: Build Singularity image
        run: |
          docker run --rm --privileged \
            -v ${{ github.workspace }}:/work \
            -w /work \
            quay.io/singularity/singularity:v3.10.4 \
            build lupus-workflow.sif lupus-workflow.def

      - name: Login and push to GHCR (OCI format)
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Get repository name in lowercase
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')

          # Generate tag
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            TAG="latest"
          else
            TAG="${{ github.ref_name }}"
          fi

          # Login using Singularity via Docker
          echo "$GHCR_TOKEN" | docker run --rm -i --privileged \
            -v ${{ github.workspace }}:/work \
            -w /work \
            quay.io/singularity/singularity:v3.10.4 \
            remote login -u ${{ github.actor }} --password-stdin oras://ghcr.io

          # Push using Singularity via Docker
          docker run --rm --privileged \
            -v ${{ github.workspace }}:/work \
            -w /work \
            quay.io/singularity/singularity:v3.10.4 \
            push lupus-workflow.sif oras://ghcr.io/${REPO_LOWER}:${TAG}-singularity

      - name: Upload Singularity image as artifact
        uses: actions/upload-artifact@v4
        with:
          name: lupus-workflow-singularity
          path: lupus-workflow.sif
          retention-days: 30
